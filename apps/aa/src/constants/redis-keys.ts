// Redis cache keys and TTLs for disbursement operations
export const DISBURSE_CACHE_TTL = 300;
export const DISBURSE_CACHE_KEY_PREFIX = 'disburse:progress:';

// Redis cache keys and TTLs for group cash transfer operations
export const GCT_CACHE_TTL = 300;
export const GCT_CACHE_KEY_PREFIX = 'gct:progress:';
